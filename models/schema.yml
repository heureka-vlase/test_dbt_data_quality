version: 2

sources:
  - name: budget
    schema: out.c-CDT_Plan
    quoting:
      database: true
      schema: true
      identifier: true
    tables:
      - name: int_daily_plan
        columns:
          - name: model_name
            tests:
              # The 'not_null' test has been removed
              - accepted_values:
                  values: ['week cycle', 'reality', 'classic', 'payday window']
          - name: service_id
            tests:
              # The 'not_null' test has been removed
              - accepted_values:
                  values: [1, 2, 101, 103, 106, 201, 202, 203, 204]
